version: 2.1

# -------------------------------------------------------------------------------------
# DrumCharter CI/CD Configuration (CircleCI)
# -------------------------------------------------------------------------------------

orbs:
  browser-tools: circleci/browser-tools@1.5.0
  node: circleci/node@6.0.0

jobs:
  # -----------------------------------------------------------------------------------
  # 1. LINTING
  # -----------------------------------------------------------------------------------
  lint:
    executor:
      name: node/default
      tag: "22.14.0"
    steps:
      - checkout
      - node/install-pnpm
      - run:
          name: Install dependencies
          command: pnpm install
      - run:
          name: Run Biome lint
          command: pnpm lint
      - run:
          name: Run Markdown lint
          command: pnpm lint:md

  # -----------------------------------------------------------------------------------
  # 2. UNIT TESTS
  # -----------------------------------------------------------------------------------
  test-unit:
    executor:
      name: node/default
      tag: "22.14.0"
    steps:
      - checkout
      - node/install-pnpm
      - run:
          name: Install dependencies
          command: pnpm install
      - run:
          name: Run Vitest
          command: pnpm test run

  # -----------------------------------------------------------------------------------
  # 3. E2E & PWA TESTS (Supabase + Playwright)
  # -----------------------------------------------------------------------------------
  test-e2e:
    machine:
      image: ubuntu-2204:2024.01.1
    resource_class: large
    working_directory: ~/repo
    steps:
      - checkout
      - run:
          name: Install Node.js 22 and pnpm
          command: |
            # On machine executor, nvm is pre-installed
            export NVM_DIR="$HOME/.nvm"
            [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
            nvm install 22
            nvm use 22
            npm install -g pnpm
            echo 'export PATH=$(npm config get prefix)/bin:$PATH' >> $BASH_ENV
            echo 'export NVM_DIR="$HOME/.nvm"' >> $BASH_ENV
            echo '[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"' >> $BASH_ENV
            echo 'nvm use 22 > /dev/null' >> $BASH_ENV
      - run:
          name: Install dependencies
          command: |
            source $BASH_ENV
            pnpm install
      - run:
          name: Install Playwright Browsers
          command: |
            source $BASH_ENV
            pnpm exec playwright install --with-deps chromium
      - run:
          name: Setup Docker Permissions
          command: |
            sudo chmod 666 /var/run/docker.sock
            docker version
      - run:
          name: Start Supabase (local)
          command: |
            source $BASH_ENV
            npx supabase start --no-backup
            # Wait for healthy - checking the REST API specifically
            echo "Waiting for Supabase services to be healthy..."
            HEALTHY=false
            for i in {1..30}; do
              if curl -s http://127.0.0.1:54321/rest/v1/ > /dev/null; then
                echo "Supabase is ready"
                HEALTHY=true
                break
              fi
              echo "Waiting for services... ($i/30)"
              # Check container status for diagnostics
              docker ps --format '{{.Names}}: {{.Status}}' | grep supabase || true
              sleep 10
            done
            if [ "$HEALTHY" != "true" ]; then
              echo "Supabase failed to start"
              docker ps -a
              exit 1
            fi
            SUPABASE_STATUS=$(npx supabase status -o json)
            echo "export NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321" >> $BASH_ENV
            echo "$SUPABASE_STATUS" | jq -r '"export NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=" + .ANON_KEY' >> $BASH_ENV
            echo "$SUPABASE_STATUS" | jq -r '"export SUPABASE_SERVICE_ROLE_KEY=" + .SERVICE_ROLE_KEY' >> $BASH_ENV
      - run:
          name: Build Next.js app
          command: |
            source $BASH_ENV
            pnpm cross-env NEXT_PUBLIC_FORCE_SW=true pnpm build
      - run:
          name: Run Playwright E2E Tests
          no_output_timeout: 10m
          command: |
            source $BASH_ENV
            export SKIP_BUILD=true
            pnpm test:e2e -- --project=chromium --trace on
          environment:
            CI: "true"
      - store_artifacts:
          path: test-results
          destination: playwright-results

  # -----------------------------------------------------------------------------------
  # 4. DATABASE SYNC (Supabase Migrations)
  # -----------------------------------------------------------------------------------
  deploy-migrations:
    executor:
      name: node/default
      tag: "22.14.0"
    steps:
      - checkout
      - node/install-pnpm
      - run:
          name: Install dependencies
          command: pnpm install
      - run:
          name: Push Migrations to Staging
          command: |
            if [ "$CIRCLE_BRANCH" = "staging" ]; then
              pnpm supabase link --project-ref $SUPABASE_PROJECT_ID_STAGING --password $SUPABASE_DB_PASSWORD_STAGING
              pnpm supabase db push --password $SUPABASE_DB_PASSWORD_STAGING
            fi
          environment:
            SUPABASE_ACCESS_TOKEN: $SUPABASE_ACCESS_TOKEN
      - run:
          name: Push Migrations to Production
          command: |
            if [ "$CIRCLE_BRANCH" = "main" ]; then
              pnpm supabase link --project-ref $SUPABASE_PROJECT_ID_PROD --password $SUPABASE_DB_PASSWORD_PROD
              pnpm supabase db push --password $SUPABASE_DB_PASSWORD_PROD
            fi
          environment:
            SUPABASE_ACCESS_TOKEN: $SUPABASE_ACCESS_TOKEN

# -------------------------------------------------------------------------------------
# WORKFLOW DEFINITIONS
# -------------------------------------------------------------------------------------
workflows:
  version: 2
  build-test-deploy:
    jobs:
      - lint
      - test-unit
      - test-e2e:
          requires:
            - lint
            - test-unit
      - deploy-migrations:
          requires:
            - test-e2e
          filters:
            branches:
              only:
                - main
                - staging
