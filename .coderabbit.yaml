# CodeRabbit configuration for DrumCharter
# Docs: https://docs.coderabbit.ai/reference/configuration

reviews:
  # Path filters to ignore specific directories
  # Use '!' prefix to exclude paths
  path_filters:
    - '!**/.agents/**'
    - '!**/.junie/**'
