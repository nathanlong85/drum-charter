# Pnpm configuration for drum-charter

# Allow symlinks in node_modules (required for some tools)
symlink=true

# Increase timeout for installations that might take longer
fetch-timeout=120000
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
